# this file calls the functions in geometry.py
# we import a module that we have created ourselves.

import geometry as geo

triangle1_area = geo.trianglearea(3,5)
print(triangle1_area)

print(geo.squarearea(10))

print(geo.trianglecircumference(2,3,5))
